{{define "title"}}Inbox{{end}}
{{define "content"}}
  <ol>
    {{range .}}
      <li class="preview{{ if .Flag.Seen }} read{{ end }}">
        <a href="/mail/{{.ID}}">{{ index .FromName }} {{ .Subject }}</a>
      </li>
    {{else}}
      <li>No messages</li>
    {{end}}
  </ul>
{{end}}
