{{define "title"}}Inbox{{end}}
{{define "content"}}
  {{range .}}
    <a href="/mail/{{.ID}}" class="preview{{ if .Flag.Not 'S' }} read{{ end }}">
      <span>{{ index .FromName }}</span>
      <span>{{ .Subject }}</span>
    </a>
  {{else}}
    <p>No messages</p>
  {{end}}
{{end}}
